import type { Metadata } from 'next'; import Link from 'next/link'; import { notFound } from 'next/navigation'; import { ContentPage } from '@/components/ContentPage'; import { guidesByLocale } from '@/lib/content'; import { copy,isLocale,locales } from '@/lib/i18n'; import { pageCopy } from '@/lib/page-copy';
export function generateStaticParams(){return locales.map(locale=>({locale}))}
export async function generateMetadata({params}:{params:Promise<{locale:string}>}):Promise<Metadata>{const {locale}=await params;if(!isLocale(locale))return{};return{title:`${pageCopy[locale].guidesTitle} | ${copy[locale].siteName}`,description:pageCopy[locale].guidesIntro,alternates:{canonical:`/${locale}/guides/`,languages:{en:'/en/guides/',ko:'/ko/guides/',ja:'/ja/guides/',es:'/es/guides/','x-default':'/en/guides/'}}}}
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const p=pageCopy[locale];return <ContentPage locale={locale}><header className="pageHero"><p className="eyebrow">GUIDES</p><h1>{p.guidesTitle}</h1><p>{p.guidesIntro}</p></header><div className="articleGrid">{guidesByLocale[locale].map(g=><article className="articleCard" key={g.slug}><h2>{g.title}</h2><p>{g.description}</p><Link href={`/${locale}/guides/${g.slug}/`}>{p.read} →</Link></article>)}</div></ContentPage>}
